<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="with=device.with, initial-scale=1">
    <title>Shirizu</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#"><%= image_tag "logo.png", style: 'with : 120%; height : 120%'%></a>
      </div>
      <div class="collapse navbar-collapse" id="myNavbar">
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to '', root_path, :class => 'glyphicon glyphicon-home'%></li>
          <li><a href="#"></span> Explore</a></li>
          <li><a href="#"></span> About</a></li>
          <% if user_signed_in? %>
            <li><%= link_to current_user.username, edit_user_registration_path, :class => 'glyphicon glyphicon-user'%></li>
            <li><%= link_to ' Déconnexion', destroy_user_session_path, method: :delete, :class => 'glyphicon glyphicon-log-out' %></li>
          <% else %>
          <li><%= link_to ' Connexion', new_user_session_path, :class => 'glyphicon glyphicon-log-in' %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
    <div class="container">
        <%= link_to 'Séries', shows_path, class: 'btn btn-default' %>
        <%= link_to 'Sociétés', companies_path, class: 'btn btn-default' %>
        <%= link_to 'Saisons', seasons_path, class: 'btn btn-default' %>
        <%= link_to 'Episodes', episodes_path, class: 'btn btn-default' %>
      <%= yield %>
    </div>
  </body>
</html>
